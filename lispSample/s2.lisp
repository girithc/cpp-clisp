//(define fun (a b)
    //(print  (cdr (cons (cons a b) (cons b a)) ))
 
    //(car b)
    //(car a)
//)

//(print (fun "yes" "no"))

(set a "2")
(print a)
(set b (cons a a))
(print b)
(set c (cons a b))
(print c)